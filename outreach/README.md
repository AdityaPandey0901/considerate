# Outreach drafts (H1 / H2) — nothing here has been sent anywhere

This directory holds drafts for the two non-code items on the backlog:

- `draft-pandey-considerate-00.txt` — an IETF Internet-Draft skeleton
  (Informational track, individual submission format) adapting SPEC.md
  into RFC-style prose. **Not submitted to the IETF datatracker.**
  Submitting one is a real, public, findable action under a real name
  that's awkward to fully retract — do that deliberately, not as a
  side effect of a repo cleanup pass.
- `issue-browser-use.md`, `issue-langchain.md`, `issue-crewai.md` —
  drafts for GitHub issues/discussions on those three projects, proposing
  native adoption of the `Considerate-Agent` behavior contract.
  **Not posted to any of those repos.**

All four need a maintainer's deliberate decision to send, not a repo
commit, since posting on someone else's project or submitting a document
to a standards body speaks in the maintainer's name to a community that
can't be un-notified.
